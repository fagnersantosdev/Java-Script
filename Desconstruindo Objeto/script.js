let pessoa = {
    nome: "Myname",
    sobrenome: "MyLastName",
    idade: 23,
    empresa: "My work"
}

const {nome, idade} = pessoa;

console.log(nome);

//Sempre que eu quiser que o conteúdo apareça no meu console, devo me referrir no CONST, ou seja, transforma-lo em uma variável para imprimi-lo no console.log